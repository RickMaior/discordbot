const settings = require("../bot/settings.json");

const fs = require("fs");
const ms = require("ms");

module.exports = async (bot ,info) => {
 // console.log("info= ");
  //console.log(info);

};

module.exports.help = {
  // when the bot joins one server
  name: "Raw",
  event: "raw"
};
